Add Earbud controls screen (gesture config)

- New row above App update opens it: bud icon, Left/Right per-bud
  selector, then one row per gesture.
- Tapping a gesture lists the actions it may take, per the spec.
  Tap and hold is multi-select and cycles through what you pick.
- No on-call section: not implemented by design.
- Selections save on the phone only. Sending them needs the action
  values, which are still unconfirmed, so the buds keep their
  current bindings and the screen says so.
